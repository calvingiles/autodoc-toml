{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "command -v hatch >/dev/null 2>&1 || uv tool install hatch && hatch run setup",
            "description": "Install hatch and pre-commit hooks for local development"
          }
        ]
      }
    ]
  }
}
